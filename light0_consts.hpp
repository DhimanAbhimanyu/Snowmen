#ifndef LIGHT0_CONSTS_HPP
#define LIGHT0_CONSTS_HPP

#include "vectors.hpp"
#include "colors.hpp"

static const Vector4f LIGHT0_POSITION = Vector4f(1, 0.667);

static const Color4f LIGHT0_AMBIENT = Color4f(0, 0, 0, 1);
static const Color4f LIGHT0__DIFFUSE = Color4f(1, 1, 1, 1);
static const Color4f LIGHT0_SPECULAR = Color4f(1, 1, 1, 1);

#endif
